# Workflow: Executive Review

**مرحله:** ۴ از ۶
**وضعیت:** پس از Challenge

**ورودی:** فایل‌های `recommendation_*.md` و `challenge_report_*.md`.

**فرآیند:**
1. فراخوانی `/executive-review` و `/prepare-decision`
2. ایجنت `chief-of-staff` مهارت `executive-decision-package` را فراخوانی می‌کند.
3. پرونده نهایی تصمیم، شامل تحلیل، پیشنهاد، چالش و جایگزین، آماده می‌شود.

**خروجی:** فایل `decision_package_[timestamp].md` که به مرحله بعد (`workflows/decision/`) ارسال می‌شود.
