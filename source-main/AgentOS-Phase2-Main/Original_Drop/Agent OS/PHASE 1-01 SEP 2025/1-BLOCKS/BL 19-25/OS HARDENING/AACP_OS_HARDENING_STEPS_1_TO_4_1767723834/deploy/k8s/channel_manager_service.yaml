apiVersion: apps/v1
kind: Deployment
metadata:
  name: channel_manager_service
  namespace: aacp-phase1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: channel_manager_service
  template:
    metadata:
      labels:
        app: channel_manager_service
    spec:
      containers:
        - name: channel_manager_service
          image: aacp/channel_manager_service:v1
          ports:
            - containerPort: 8000
          env:
            - name: AACP_DEV_SECRET
              valueFrom:
                secretKeyRef:
                  name: aacp-dev-secret
                  key: secret
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: channel_manager_service
  namespace: aacp-phase1
spec:
  selector:
    app: channel_manager_service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
