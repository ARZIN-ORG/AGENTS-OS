Version Compatibility Matrix

AACP Core:
- v1.x compatible with Phase-1 Freeze

SDK:
- must declare supported AACP versions
- must enforce audit envelope v1 mandatory fields
- must reject unknown policy scopes by default