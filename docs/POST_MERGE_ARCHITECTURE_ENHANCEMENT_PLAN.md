# طرح جامع بهبود و زیباسازی معماری AgentOS (Post-Merge Action Plan)

**نسخه:** v1.0 (عملیاتی)
**تاریخ:** ۲۰۲۶-۰۸-۰۵
**هدف:** تبدیل معماری فعلی به یک پلتفرم جذاب، شکیل، ماژولار و بسیار قابل فهم برای تیم‌های جدید و ذی‌نفعان.
**محل فایل:** `/AGENTS-OS/POST_MERGE_ARCHITECTURE_ENHANCEMENT_PLAN.md`

---

## بخش ۱: یکپارچه‌سازی ۱۱ ایجنت با معماری (The Glue Integration)
پس از الحاق، ایجنت‌های `.claude/agents` باید به عنوان «نقاط تصمیم‌گیری» به موتور اجرا متصل شوند.

**اقدامات فوری:**
1. **ایجاد یک Identity Map (نقشه هویت):** فایل `identities/AGENT_IDENTITY_REGISTRY.md` را با مرجع `BL04` (سرویس رجیستری ایجنت) همگام‌سازی کنید تا هر ۱۱ ایجنت دارای یک `Agent ID` یکتا در پایگاه داده کافکا باشند.
2. **پیاده‌سازی «Agent Bridge»:** یک سرویس پایتون کوچک (`src/services/core/agent_bridge/`) بنویسید که خروجی توصیه‌های `.claude/agents` را به فرمت JSON رویداد کافکا ترجمه کرده و به `BL17` (Intent Gateway) ارسال کند.
3. **تست اتصال:** یک تابع تست بنویسید که تأیید کند یک ایجنت در `.claude` می‌تواند یک "پیشنهاد" تولید کرده و در داشبورد `UI/` نمایش داده شود.

---

## بخش ۲: زیباسازی ظاهری و شکیل‌سازی معماری (Making it Polished & Aesthetic)

برای اینکه سیستم دیگر "خام" به نظر نرسد، باید استانداردهای بصری و ساختاری زیر اعمال شوند:

**الف) استانداردسازی فایل‌های کانفیگ:** 
- یک فایل روت `Makefile` در ریشه‌ی ریپو ایجاد کنید. این فایل به تیم اجازه می‌دهد با دستورات ساده `make build`، `make up` و `make test` کل پروژه را مدیریت کنند (به جای اینکه هر بار مسیرهای عمیق را تایپ کنند).
- فایل‌های `docker-compose.dev.yml` و `docker-compose.prod.yml` را در پوشه `deployment/` با نام‌گذاری واضح (مثلاً `core.yml` برای سرویس‌های هسته و `ui.yml` برای سرویس UI) تفکیک کنید.

**ب) جداسازی بصری در UI (قابلیت برندینگ):**
- در پوشه `ui/themes/`، یک پوشه به نام `_shared/` ایجاد کنید که شامل فایل‌های CSS و JS مشترک بین همه تم‌ها باشد تا از تکرار کد جلوگیری شود.
- یک صفحه `index.html` در روت `ui/` ایجاد کنید که بر اساس پارامتر `?theme=` بتواند بین `theme-arzin` و `theme-pedramflow` سوئیچ کند (بدون نیاز به فایل‌های جداگانه `indexarzin.html`).

**ج) حذف آشفتگی‌های فنی (Deep Clean):**
- پس از اطمینان از استخراج سرویس‌های `BL13` و `BL17` از `SUPER_BUNDLE`، تمام پوشه‌های `Original_Drop`، `PHASE 1` و `AACP_PHASE1_...` را از ریپو حذف کنید. این پوشه‌ها فقط برای بایگانی بوده‌اند و در ریپوی نهایی باعث سردرگمی می‌شوند.

---

## بخش ۳: قابل فهم‌تر کردن معماری برای تیم‌های جدید (Making it Comprehensible)

«فهم‌پذیری» کلید اصلی است. برای اینکه هر توسعه‌دهنده جدیدی بتواند ظرف ۳۰ دقیقه معماری را درک کند:

**۱. ایجاد یک «فرهنگ لغت بلوک‌های ساختمانی» (Block Dictionary):**
در روت ریپو، یک فایل `BLOCK_GLOSSARY.md` ایجاد کنید. در این فایل، برای هر بلوک (`BL01` تا `BL25`) یک توضیح ساده و یک جمله‌ای با قالب زیر بنویسید:
> *BL01 (Audit Envelope): "مهر و موم امنیتی هر پیام که ثابت می‌کند چه کسی آن را فرستاده و چه زمانی".*

**۲. ترسیم یک «نقشه‌ی جریان داده بصری» (ASCII Flowchart):**
یک فایل `ARCHITECTURE_FLOW.md` در روت ایجاد کنید که مسیر یک درخواست را از لحظه‌ی ورود کاربر در `UI` تا ثبت نهایی در `Audit Sink` با استفاده از دیاگرام متنی (ASCII) نشان دهد. مثال:
```text
User (UI) → Intent (BL17) → Recommendation (BL13) → Agent (`.claude`) → Policy Check (BL07) → Permit (BL08) → Kafka → Execution Log
```

۳. مستندسازی محیط توسعه (Dev Environment):
در یک فایل DEVELOPMENT.md در روت، دقیقاً مشخص کنید:

· چه پکیج‌های پایتونی روی سیستم باید نصب شوند (pip install -r src/shared/requirements.txt).
· نحوه‌ی اجرای سرویس‌ها بدون داکر (برای دیباگ سریع).
· نحوه‌ی دسترسی به لاگ‌ها (tail -f logs/*).

---

بخش ۴: استقرار و بهره‌برداری در محیط واقعی (Production Readiness)

برای اینکه معماری جذاب به نظر برسد، باید بتواند به راحتی روی سرورهای ابری مستقر شود.

۱. ساخت فایل استقرار ابری:

· یک پوشه deployment/cloud/ ایجاد کنید.
· فایل k8s-deployment.yaml و k8s-service.yaml را برای هر یک از سرویس‌های هسته (BL01-08) و سرویس‌های دامنه (BL13, BL17) و سرویس کنسول (BL19) بنویسید.
· برای سرویس‌های UI، از یک nginx.conf ساده برای سرو کردن فایل‌های استاتیک در محیط تولید استفاده کنید.

۲. سیستم مانیتورینگ مرکزی (Central Observability):

· فایل prometheus.yml (برای جمع‌آوری متریک‌های سرویس‌های پایتون) و filebeat.yml (برای ارسال لاگ‌ها به Elasticsearch) را به پوشه deployment/monitoring/ اضافه کنید.
· در فایل CLAUDE.md به این موضوع اشاره کنید که "برای مشاهده وضعیت سیستم، به داشبورد Grafana مراجعه کنید".

۳. تست انسجام یکپارچه:

· یک اسکریپت به نام scripts/integrity_check.sh بنویسید که با استفاده از curl تمام پورت‌های سرویس‌ها را تست کند و اگر یکی پاسخ نداد، خطا را با رنگ قرمز در ترمینال گزارش دهد.

---

بخش ۵: جدول گام‌های اجرایی (Matrix of Execution)

اولویت گام توضیح مستند مرتبط
۱ ایجاد Makefile و حذف باندل‌های Super_Bundle ریپو را تمیز کرده و دستورات را کوتاه می‌کند. ARCHITECTURE_AND_OPERATIONS.md
۲ ایجاد BLOCK_GLOSSARY.md و ARCHITECTURE_FLOW.md هر توسعه‌دهنده‌ی جدیدی می‌تواند معماری را در ۵ دقیقه بفهمد. BLOCK_GLOSSARY.md
۳ پیاده‌سازی سرویس Agent Bridge حلقه‌ی مفقوده‌ی بین .claude/agents و BL17 را پر می‌کند. src/services/core/README.md
۴ نوشتن فایل‌های k8s-deployment.yml برای سرویس‌ها سیستم آماده‌ی استقرار در محیط Cloud می‌شود. deployment/k8s/README.md
۵ تفکیک تم‌های UI و ایجاد سوئیچ پویا UI بسیار حرفه‌ای و قابل برندینگ برای مشتریان می‌شود. ui/README.md
۶ اضافه کردن مونیتورینگ (Prometheus + Grafana) باعث می‌شود مدیر فنی بتواند با یک نگاه سلامت کل ۲۵ بلوک را ببیند. deployment/monitoring/README.md

---

نتیجه‌گیری نهایی برای تیم اجرایی:
با اجرای گام‌های ۱، ۲ و ۳ از جدول فوق (که می‌توانند در عرض یک هفته کاری توسط یک توسعه‌دهنده انجام شوند)، معماری فعلی از یک "پروژه‌ی شخصی به‌هم‌ریخته" به یک "محصول نرم‌افزاری Enterprise-Grade با قابلیت ارائه به مشتریان خارجی" تبدیل خواهد شد.
