# AGENTOS ARCHITECTURE & OPERATIONS MANUAL (نسخه عمومی)

**نسخه:** v1.0 (نهایی)
**تاریخ تدوین:** ۲۰۲۶-۰۸-۰۵
**مخاطب:** بنیان‌گذار، مدیر ارشد فناوری (CTO)، کمیته اجرایی سازمان
**محل قرارگیری در ریپو:** `/AGENTS-OS/ARCHITECTURE_AND_OPERATIONS.md`

---

## ۱. تعریف و هدف (چارچوب 6WH)

| پرسش | پاسخ |
| :--- | :--- |
| **WHAT (چیست؟)** | یک سیستم عامل کامل و یکپارچه برای اجرای «سازمان ایجنت‌محور». این سیستم، تعاریف متنی ایجنت‌ها (در `.claude/agents`) را به عملیات واقعی، قابل اجرا و قابل نظارت تبدیل می‌کند. |
| **WHY (چرا؟)** | برای تبدیل «سازمان کاغذی» (فایل‌های `.md`) به یک «موتور اجرایی زنده» که ایجنت‌ها را با یکدیگر و با انسان‌ها (کمیته) هماهنگ می‌کند. بدون این سیستم، ایجنت‌ها توانایی اجرای تصمیمات را ندارند. |
| **WHO (چه کسی؟)** | این سیستم توسط تیم فنی سازمان توسعه یافته است. مخاطبان نهایی آن، **بنیان‌گذار، CTO و کمیته اجرایی** هستند که از طریق UI بر آن نظارت می‌کنند. |
| **WHEN (کی؟)** | بلافاصله پس از استقرار در ریپوی سازمان و راه‌اندازی سرویس‌ها (بر اساس این مستند). |
| **WHERE (کجا؟)** | هسته‌ی اجرایی در ریپوی اصلی سازمان مستقر می‌شود. محیط اجرای واقعی شامل `src/core_services`، `ui` و `deployment` است. |
| **HOW (چگونه؟)** | با استفاده از معماری میکروسرویس مبتنی بر Python، پیام‌رسانی Kafka، رابط کاربری SPA (Single Page Application) و یکپارچه‌سازی از طریق فایل‌های `docker-compose.yml`. |

---

## ۲. معماری جامع (لایه‌ها و 6WH آن‌ها)

سیستم عامل ایجنت (AgentOS) به‌صورت **سه‌لایه‌ای (3-Tier Architecture)** طراحی شده است:

### لایه ۱: لایه تعریف و حاکمیت (Definition & Governance Layer)
- **چیست؟** قوانین، هویت‌ها و گردش‌های کاری که چارچوب سازمان را مشخص می‌کنند.
- **چرا؟** برای تعیین اینکه "چه کسی باید چه کاری انجام دهد" و "با چه محدودیت‌هایی" (Rule 01, 02, 03).
- **کجاست؟** در پوشه‌های `.claude/`، `governance/` و `workflows/` در روت ریپو.
- **چه کسی؟** ایجنت‌ها و کمیته‌ی اجرایی.
- **چگونه؟** توسط فایل `CLAUDE.md` و فایل‌های `.md` در `.claude/agents/` تعریف می‌شود.

### لایه ۲: لایه هسته‌ی اجرایی (Runtime & Core Engine)
- **چیست؟** موتور پردازش و هماهنگی که درخواست‌های کاربر را دریافت کرده، آن‌ها را به ایجنت‌ها ارسال می‌کند و تصمیمات تأییدشده را اجرا می‌نماید.
- **چرا؟** برای پردازش رویدادها، اعمال امنیت، ارسال پیشنهادات به UI و ثبت لاگ‌های حسابرسی.
- **کجاست؟** در پوشه‌های `src/core_services/` (برای بلوک‌های ۰۱-۱۸) و `src/governance_console/` (برای بلوک ۱۹).
- **چه کسی؟** سرویس‌های پایتون (که توسط تیم فنی نگهداری می‌شوند) و خود ایجنت‌ها.
- **چگونه؟** با استفاده از معماری رویدادمحور (Kafka) و فراخوانی `main.py`های اختصاصی.

### لایه ۳: لایه رابط کاربری و نظارت انسانی (UI & Human Oversight Layer)
- **چیست؟** یک داشبورد تحت وب که به انسان‌ها (مدیران ارشد) اجازه می‌دهد وضعیت سیستم را ببینند، درخواست ثبت کنند و تصمیمات را تأیید یا رد نمایند.
- **چرا؟** برای رعایت دقیق `Rule 01` ("Agent تصمیم نهایی نمی‌گیرد"). اجرای تصمیمات نهایی باید توسط انسان انجام شود.
- **کجاست؟** در پوشه‌ی `ui/` (که شامل `index.html`، `views/`، `themes/` و `core/` است).
- **چه کسی؟** بنیان‌گذار، CTO و مدیر اجرایی.
- **چگونه؟** از طریق اتصال به `governance-console-api` (در لایه‌ی ۲) و نمایش اطلاعات در مرورگر.

---

## ۳. بخش‌های اجرایی (BL01 تا BL25: چیستی و کارکرد)

این نام‌گذاری (BL = Block) نشان‌دهنده‌ی تقسیم‌بندی ماژولار در هسته‌ی اجرایی (Runtime) است. بر اساس معماری موجود، این بلوک‌ها به سه دسته‌ی عملکردی تقسیم می‌شوند:

### الف) فاز ۱: لایه زیرساخت و امنیت (BL01 تا BL08)
این بلوک‌ها، پایه و اساس موتور رویدادمحور (Event-Driven) را می‌سازند:
- **BL01 (Audit Envelope):** ایجاد پاکت حسابرسی برای هر پیام جهت تضمین قابلیت ردیابی.
- **BL02 (Reject & DLQ):** مدیریت پیام‌های خطا و انتقال آن‌ها به صف پیام‌های مرده (Dead Letter Queue) برای تحلیل علت.
- **BL03 (Keystore & Signature):** مدیریت امضاهای دیجیتال برای تأیید هویت پیام‌ها و جلوگیری از دستکاری.
- **BL04 (Registry & Policy):** ثبت‌نام ایجنت‌ها و تطابق پیام‌ها با خط‌مشی‌های تعریف‌شده.
- **BL05 (Channel Manager):** مدیریت کانال‌های ارتباطی کافکا.
- **BL06 (Observability):** ایجاد لاگ‌های مشاهده‌پذیر برای مانیتورینگ.
- **BL07 (Message Codec):** رمزگشایی و رمزگذاری پیام‌ها در لایه‌ی انتقال.
- **BL08 (Permit & Audit):** سرویس نهایی مجوز اجرا و ارسال رویدادها به پایگاه حسابرسی.

### ب) فاز ۲: لایه منطق و دامنه (BL13 و BL17)
این بلوک‌ها منطق تصمیم‌گیری ایجنت‌ها را پیاده‌سازی می‌کنند:
- **BL13 (Recommendation Plane):** تولید "توصیه‌های عملیاتی" بر اساس ورودی‌های ایجنت‌های تعریف‌شده در `.claude/agents/`.
- **BL17 (Intent Gateway):** دروازه‌ای که "نیت/درخواست" کاربر (انسان) را دریافت کرده و آن را به یک دستور قابل فهم برای سیستم تبدیل می‌کند.

### ج) فاز ۳: لایه حاکمیت و مدیریت (BL19 تا BL25)
این بلوک‌ها، پل ارتباطی میان سیستم و انسان هستند:
- **BL19 (Governance Console):** یک سرویس API `Read-Only` که از احراز هویت OIDC پشتیبانی کرده و داده‌های لازم برای داشبورد UI را فراهم می‌کند.
- **BL20-25:** بلوک‌های مربوط به سخت‌افزاری و عملیاتی (که در کاوش فعلی به‌عنوان فاز توسعه‌ی آینده یا فایل‌های پشتیبان مشاهده شده‌اند).

---

## ۴. ساختار انعطاف‌پذیر UI (با دو شمای متفاوت)

پوشه‌ی `ui/` به‌صورت **کاملاً پویا و ماژولار** طراحی شده است. بر اساس فایل‌های موجود، دو شمای (تم) اصلی در سیستم تعبیه شده‌اند که می‌توانید آن‌ها را مطابق برند سازمان خود تغییر دهید:

1.  **شمای A** (با دو نسخه زبان بین‌المللی و داخلی)
2.  **شمای B** (با دو نسخه زبان بین‌المللی و داخلی)

این ساختار در پوشه‌ی `ui/themes/` به این شکل دیده می‌شود:
```text
ui/themes/
├── template-a-ir/   (شامل `index_template_a.html`)
├── template-a-us/
├── template-b-ir/   (شامل `index_template_b.html`)
└── template-b-us/
