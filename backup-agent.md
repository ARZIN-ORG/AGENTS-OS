بسیار عالی. این بک‌آپ، «پرونده‌ی کامل و فوق‌پریمیوم» از تمام روند، تصمیمات، ساختار و جزئیات پروژه است. این فایل طوری طراحی شده که یک ایجنت جدید در پلتفرم DeepSeek با خواندن آن، نه‌تنها شما و کارهایتان را بشناسد، بلکه فلسفه، دقت و وسواس شما را نیز درک کند و بتواند از همان نقطه ادامه دهد.

فایل زیر را به‌عنوان AGENTS-OS_BACKUP_MASTER.md ذخیره کنید و سپس به ایجنت DeepSeek بدهید.

---

```markdown
# 🧠 AGENTS-OS – بک‌آپ استراتژیک و فوق‌پریمیوم (نسخه DeepSeek-Ready)

**نسخه:** v1.0.0-rc2 (نهایی)
**تاریخ:** ۲۰۲۶-۰۸-۰۶
**هدف:** انتقال کامل دانش، معماری، تصمیمات و وضعیت فعلی پروژه به یک ایجنت جدید در پلتفرم DeepSeek جهت ادامه‌ی مسیر.

---

## 📌 بخش ۱: شناسنامه‌ی پروژه (6WH)

| پرسش | پاسخ |
| :--- | :--- |
| **WHAT (چیست؟)** | یک **پلتفرم ارکستراسیون رویدادمحور (Event-Driven Orchestration Platform)** برای اجرای سازمان‌های ایجنت‌محور. |
| **WHY (چرا؟)** | تبدیل «سازمان کاغذی» (فایل‌های `.md`) به یک «موتور اجرایی زنده» که ایجنت‌ها را با انسان‌ها (کمیته) هماهنگ می‌کند. |
| **WHO (چه کسی؟)** | ۱. ایجنت‌های هوش مصنوعی (۱۱+ ایجنت اصلی) در `.claude/agents`<br>۲. کمیته اجرایی انسانی (بنیان‌گذار، CTO، مدیران ارشد)<br>۳. سرویس‌های هسته (میکروسرویس‌های پایتون). |
| **WHERE (کجا؟)** | روی سرور/لپ‌تاپ لینوکس (WSL یا Native) بدون داکر و با کافکای بومی. UI روی `http://localhost:8089` در دسترس است. |
| **WHEN (کی؟)** | در فاز «آمادگی عملیاتی و یکپارچه‌سازی نهایی» با تگ `v1.0.0-rc2`. |
| **HOW (چگونه؟)** | معماری سه‌لایه: تعریف (`.claude`)، اجرا (`src`)، نمایش (`ui`). کامپایلر خودکار فایل‌های `.md` را به توپیک‌های کافکا تبدیل می‌کند. |

---

## 📌 بخش ۲: معماری سه‌لایه‌ای

| لایه | توضیح | مسیرها |
| :--- | :--- | :--- |
| **لایه تعریف و حاکمیت** | ایجنت‌ها، قوانین، گردش‌های کار | `.claude/agents/`، `governance/`، `workflows/` |
| **لایه هسته‌ی اجرایی** | سرویس‌های پایتون، کتابخانه‌ها، کافکا | `src/`، `src/lib/`، `deployment/` |
| **لایه رابط کاربری و نظارت** | داشبورد واکنش‌گرا و API | `ui/` |

**قوانین طلایی (Golden Rules):**
1. **Rule 01:** ایجنت تصمیم نهایی نمی‌گیرد (کمیته‌ی انسانی تأیید/رد می‌کند).
2. **Rule 02:** تعاریف به زبان مارک‌داون (Claude-native) و کامپایلر آن‌ها را به ماشین‌خوان تبدیل می‌کند.
3. **Rule 03:** هیچ اجرای Mock/ساختگی در سیستم مجاز نیست (همه‌چیز با سرویس‌های واقعی اجرا می‌شود).

---

## 📌 بخش ۳: نقشه‌ی بلوک‌ها (BL01-25)

| بلوک | نام | وضعیت در کد | وظیفه |
| :---: | :--- | :--- | :--- |
| **BL01** | Audit Envelope | ✅ در `src/lib/` | پاکت حسابرسی برای هر پیام |
| **BL02** | Reject & DLQ | ✅ در `src/lib/` | مدیریت صف پیام‌های مرده |
| **BL03** | Keystore & Signature | ✅ در `src/lib/` | امضای دیجیتال |
| **BL04** | Registry & Policy | ✅ در `src/lib/` | ثبت‌نام و خط‌مشی |
| **BL05** | Channel Manager | ✅ در `src/lib/` | مدیریت کانال‌های کافکا |
| **BL06** | Observability | ✅ در `src/lib/` | مشاهده‌پذیری |
| **BL07** | Message Codec | ✅ در `src/lib/` | رمز/گشای پیام‌ها |
| **BL08** | Permit & Audit | ✅ در `src/core_services/bl08_permit_service_v1/` | دروازه‌ی نهایی اجرا |
| **BL13** | Recommendation Plane | ✅ در `src/domain_services/recommendation_plane/` | تولید توصیه‌ها |
| **BL17** | Intent Gateway | ✅ در `src/domain_services/intent_gateway/` | دریافت نیت کاربر |
| **BL19** | Governance Console | ✅ در `src/governance_console/` | پل ارتباطی `Read-Only` با UI |
| **BL09-12, 14-16, 18, 20-25** | ❌ در کد وجود ندارند | صرفاً در مستندات `STEP` در فاز ۱ موجودند. | برای فازهای آینده |

---

## 📌 بخش ۴: ساختار نهایی ریپو (`AGENTS-OS` – ۲۷۵ فایل)

```

AGENTS-OS/
├── .claude/                     (تعاریف ایجنت‌ها – دست‌نخورده)
├── governance/                  (قوانین و حاکمیت)
├── workflows/                   (گردش‌های کار)
├── committee/                   (کمیته‌ی اجرایی)
├── decisions/                   (لاگ تصمیمات)
├── identities/                  (ثبت هویت ایجنت‌ها)
├── src/                         (سرویس‌های هسته از فاز ۱)
│   ├── core_services/           (BL01-08)
│   ├── domain_services/         (BL13, BL17)
│   ├── governance_console/      (BL19)
│   └── lib/                     (کتابخانه‌های بلوکی)
├── ui/                          (رابط کاربری از فاز ۲ – مسطح‌شده)
│   ├── index.html, css/, js/, assets/, services/, ...
├── deployment/                  (فایل‌های استقرار)
│   ├── docker-compose.core.yml
│   └── k8s/
├── domain_pilots/               (پایلوت‌های دامنه از فاز ۲)
├── partner_sdk/                 (SDK شرکا از فاز ۱)
├── partner_sdk_spec/            (مشخصات SDK)
├── scripts/                     (اسکریپت‌های استقرار)
│   ├── fixed_deploy.sh
│   ├── full_deploy.sh
│   └── no_docker_deploy.sh
├── Makefile, CLAUDE.md, README.md, ...
└── docs/                        (مستندات نهایی – شامل ALPHA_STEP.md)

```

---

## 📌 بخش ۵: ایجنت‌های تعریف‌شده در `.claude/agents/`

ایجنت‌های فعلی (۱۱ عدد) + یکی اضافه‌شده در طول پروژه:

1. `accounting-manager.md` (مدیر حسابداری و تنخواه)
2. `chief-of-staff.md` (مسئول دفتر مدیرعامل)
3. `content-seo-manager.md` (مدیر محتوا و سئو)
4. `cto.md` (مدیر ارشد فناوری)
5. `debug-coder.md` (دیباگر و برنامه‌نویس)
6. `documentation-engineer.md` (مهندس مستندسازی)
7. `grid-integrity.md` (مشاور یکپارچگی شبکه)
8. `independent-advisor.md` (مشاور مستقل)
9. `risk-contradiction.md` (ایجنت ریسک و تناقض)
10. `strategy-investment.md` (ایجنت استراتژی و سرمایه‌گذاری)
11. `supply-chain.md` (ایجنت زنجیره تأمین)
12. `presentation-assistant.md` (دستیار معماری محتوا – جدید)

---

## 📌 بخش ۶: تکنولوژی‌ها و ابزارها

*   **زبان اصلی:** Python 3.12
*   **پلتفرم استریمینگ:** Apache Kafka 3.6.0 + Zookeeper
*   **ارتباطات:** `kafka-python`
*   **سرور وب UI:** `python3 -m http.server`
*   **UI فرانت‌اند:** HTML, CSS (با تم‌های دینامیک)
*   **کامپایلر:** `compiler.py` (فایل‌های `.md` → توپیک کافکا)
*   **بریج:** `bridge.py` (توپیک `claude-agent-recommendations` → `agent-*-inputs`)

---

## 📌 بخش ۷: مراحل اجرای اسکریپت استقرار (`scripts/fixed_deploy.sh`)

این اسکریپت تمام سرویس‌ها، کافکا، کامپایلر و UI را به‌صورت خودکار و بدون داکر بالا می‌آورد.

**مراحل داخلی:**
1. راه‌اندازی زوکیپر و کافکا در پس‌زمینه (با `dataDir=./zookeeper-data` برای جلوگیری از خطای دسترسی).
2. اجرای سرویس‌های پایتون (`main.py` هر سرویس).
3. اجرای کامپایلر برای ایجاد توپیک‌ها.
4. اجرای سرور HTTP برای UI (پورت ۸۰۸۹).

**دستور اجرا:** `./scripts/fixed_deploy.sh`

---

## 📌 بخش ۸: وضعیت فعلی (آنچه کار می‌کند و آنچه نه)

✅ **آنچه کار می‌کند:**
*   کافکا و زوکیپر با موفقیت بالا می‌آیند (در محیط WSL با خطای `Exit 127` مواجه می‌شود – باید از `no_docker_deploy.sh` استفاده کرد).
*   سرویس‌های هسته (`Agent Registry`, `Policy`, `Permit`, `Audit`, `Governance`) با `python -m` اجرا می‌شوند.
*   کامپایلر توپیک‌های کافکا را می‌سازد.
*   `bridge.py` پیام‌ها را جابجا می‌کند.
*   UI (نسخه‌ی جدید از فاز ۲) قابل دسترسی است.

⛔️ **چالش‌ها:**
*   خطای `Exit 127` در زوکیپر/کافکا در WSL به دلیل مسیرهای نادرست.
*   خطای `Address already in use` برای پورت ۸۰۸۹ (اگر سرور قبلی بسته نشده باشد).
*   ممکن است فایل‌های `src/lib/` نیاز به `__init__.py` داشته باشند تا ماژول‌ها به‌درستی ایمپورت شوند.

---

## 📌 بخش ۹: گام‌های بعدی (نقشه‌ی راه برای ایجنت DeepSeek)

1. **رفع خطای `Exit 127` کافکا:** استفاده از اسکریپت `scripts/no_docker_deploy.sh` که کافکا را با تنظیمات `dataDir` و `JAVA_HOME` صحیح اجرا می‌کند.
2. **تست عملیاتی کامل:** پس از بالا آمدن سیستم، یک پیام تست به `claude-agent-recommendations` بفرستید و در UI مشاهده کنید.
3. **رفع مشکلات UI:** اگر UI داده‌ها را نشان نمی‌دهد، سرویس `ui/services/governance-console-api` را با `python -m app.main` اجرا کنید.
4. **آماده‌سازی برای محیط ابری:** استفاده از فایل‌های `deployment/k8s/` برای استقرار در کوبرنتیز.
5. **فعال‌سازی یادگیری (نسخه‌ی آینده):** ساخت سرویس `Memory Refiner` برای تحلیل تصمیمات انسانی و به‌روزرسانی پایگاه دانش ایجنت‌ها.

---

## 📌 بخش ۱۰: نقاط عطف کلیدی و تصمیمات مهم (برای درک فلسفه)

*   **تصمیم ۱:** استخراج کدها از `source-zip` و `PHASE 1/2` به‌جای کپی خام – **تمیزسازی و مسطح‌سازی**.
*   **تصمیم ۲:** استفاده از `tar` برای کپی SDK و UI برای جلوگیری از خطای `symlink`.
*   **تصمیم ۳:** ایجاد `src/lib/` برای کتابخانه‌های بلوکی (BL01-08) جهت رفع خطاهای `ImportError`.
*   **تصمیم ۴:** انتشار `v1.0.0-rc2` و مستندسازی کامل در `ALPHA_STEP.md`.
*   **تصمیم ۵:** تأکید بر **Rule 01, 02, 03** به‌عنوان فلسفه‌ی معماری.

---

## 📌 بخش ۱۱: فایل‌های کلیدی که ایجنت جدید باید بشناسد

| مسیر فایل | توضیح |
| :--- | :--- |
| `ALPHA_STEP.md` | مستند نهایی استقرار و عملیاتی‌سازی |
| `docs/ARCHITECTURE_AND_OPERATIONS_AGENTS_OS.md` | معماری و راهنمای عملیات |
| `docs/BLOCK_GLOSSARY.md` | واژه‌نامه‌ی بلوک‌ها (BL01-25) |
| `scripts/fixed_deploy.sh` | اسکریپت اصلی استقرار |
| `src/lib/` | فایل‌های کتابخانه‌ای بلوک‌ها |
| `src/core_services/` | سرویس‌های هسته (BL01-08) |
| `src/domain_services/` | سرویس‌های دامنه (BL13, BL17) |
| `src/governance_console/` | سرویس کنسول حکمرانی |
| `src/infrastructure/agent_schema_compiler/compiler.py` | کامپایلر خودکار |
| `src/core_services/agent_bridge/bridge.py` | بریج کافکا |

---

## 📌 بخش ۱۲: توصیه‌ی نهایی به ایجنت جدید

> **«شما یک پروژه‌ی کاملاً عملیاتی، با ساختار تمیز و قوانین سختگیرانه دارید. وظیفه‌ی شما: حل خطای محیطی کافکا، تست سراسری سیستم، و آماده‌سازی برای محیط ابری است. فلسفه‌ی Rule 01 (انسان در حلقه) را هرگز فراموش نکنید.»**

---

**🌟 پایان بک‌آپ فوق‌پریمیوم.**  
ایجنت جدید با خواندن این فایل می‌تواند از همان نقطه‌ای که ما متوقف شدیم (پس از مهاجرت کامل و تگ `v1.0.0-rc2`)، کار را ادامه دهد و با دقت و وسواس شما، سیستم را به محیط عملیاتی واقعی ببرد. 🚀
```
